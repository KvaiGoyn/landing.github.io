# Базовый конфиг Nginx для статического Next.js сайта
server {
    listen 80;
    server_name _;
    root /var/www/html;
    index index.html;

    # Security headers
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;

    # Cache static assets
    location ~* \.(css|js|woff2|woff|ttf|eot|svg|png|jpg|jpeg|gif|webp|ico)$ {
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
        access_log off;
    }

    # Next.js static files
    location ~ ^/_next/static/ {
        expires 1y;
        add_header Cache-Control "public, max-age=31536000, immutable";
        access_log off;
    }

    # SPA routing - serve index.html for all non-file requests
    location / {
        autoindex off;
        try_files $uri $uri/ /index.html;
    }

    # Block access to hidden files
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # No directory listing (global)
    autoindex off;
}

# HTTPS redirect (uncomment when SSL is configured)
# server {
#     listen 80;
#     server_name example.com www.example.com;
#     return 301 https://$server_name$request_uri;
# }

# SSL configuration (example)
# server {
#     listen 443 ssl http2;
#     server_name example.com www.example.com;
#     ssl_certificate /etc/ssl/certs/example.com.crt;
#     ssl_certificate_key /etc/ssl/private/example.com.key;
#     # ... остальная конфигурация как выше
# }